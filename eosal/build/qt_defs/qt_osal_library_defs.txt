#***************************************************************************************************
#
# Qmake setup to compile an OSAL library module (Nokia QT make)
#
#***************************************************************************************************

!include( qt_defs.txt ) {
   message( "No qt_defs.txt file found" )
}

QT -= core gui

DESTDIR = $${OPEN_EN_LIBPATH}

TEMPLATE = lib
CONFIG += staticlib warn_on
CONFIG -= qt
